"use client";
/* eslint-disable react-hooks/set-state-in-effect */

import Link from "next/link";
import { FormEvent, useEffect, useState } from "react";
import { ArrowRight, BookOpen, CalendarDays, FileText, LoaderCircle, Megaphone, Search, Users } from "lucide-react";
import { useSearchParams } from "next/navigation";
import { AppShell } from "@/components/app-shell";
import { AppToast } from "@/components/app-toast";
import { AuthGuard } from "@/components/auth-guard";
import { ModuleGuard } from "@/components/module-guard";
import styles from "@/components/community-suite.module.css";

type Result={id:string;type:string;title:string;description:string;href:string;meta?:string};
type ApiResult={id:string|number;type?:string;title?:string;name?:string;description?:string;excerpt?:string;href?:string;url?:string;meta?:string;createdAt?:string};
function normalize(item:ApiResult):Result{const type=item.type??"content";return{id:String(item.id),type,title:item.title??item.name??"Resultado",description:item.description??item.excerpt??"",href:item.href??item.url??"/",meta:item.meta??item.createdAt}}
function resultIcon(type:string){if(type.includes("announcement"))return <Megaphone/>;if(type.includes("unit"))return <BookOpen/>;if(type.includes("member")||type.includes("user"))return <Users/>;if(type.includes("event"))return <CalendarDays/>;return <FileText/>}
const labels:Record<string,string>={announcement:"Comunicado",curricular_unit:"Unidade curricular",document:"Documento",material:"Material",member:"Membro",event:"Evento",poll:"Inquérito",request:"Pedido"};
export function GlobalSearch(){const searchParams=useSearchParams(),initial=searchParams.get("q")??"",[query,setQuery]=useState(initial),[submitted,setSubmitted]=useState(initial),[results,setResults]=useState<Result[]>([]),[loading,setLoading]=useState(Boolean(initial)),[error,setError]=useState("");useEffect(()=>{if(!submitted.trim()){setResults([]);setLoading(false);return}const controller=new AbortController();setLoading(true);setError("");fetch(`/api/search?q=${encodeURIComponent(submitted.trim())}`,{cache:"no-store",signal:controller.signal}).then(async response=>{const data=await response.json() as {results?:ApiResult[];items?:ApiResult[];error?:string};if(!response.ok)throw new Error(data.error||"Não foi possível concluir a pesquisa.");setResults((data.results??data.items??[]).map(normalize))}).catch(reason=>{if(!(reason instanceof DOMException&&reason.name==="AbortError"))setError(reason instanceof Error?reason.message:"Não foi possível concluir a pesquisa.")}).finally(()=>{if(!controller.signal.aborted)setLoading(false)});return()=>controller.abort()},[submitted]);const submit=(event:FormEvent)=>{event.preventDefault();const value=query.trim();setSubmitted(value);const url=value?`/pesquisa?q=${encodeURIComponent(value)}`:"/pesquisa";window.history.replaceState(null,"",url)};return <AuthGuard><ModuleGuard moduleKey="search.global"><AppShell active="search" breadcrumb="Pesquisa"><div className={styles.page}><header className={styles.hero}><div className={styles.heroCopy}><span className={styles.heroIcon}><Search/></span><div><span className="eyebrow">Toda a aplicação</span><h1>Pesquisa global</h1><p>Encontra rapidamente comunicados, unidades curriculares, documentos, materiais, eventos e membros da Comissão de Curso.</p></div></div></header>{error&&<AppToast kind="error" message={error} onDismiss={()=>setError("")}/>}<section className={styles.panel}><form className={styles.toolbar} onSubmit={submit} role="search"><label className={styles.search}><Search/><span className="sr-only">Termo de pesquisa</span><input autoFocus value={query} onChange={event=>setQuery(event.target.value)} placeholder="O que procuras?" maxLength={160}/></label><button className="button button--primary" type="submit" disabled={!query.trim()||loading}>{loading?<LoaderCircle className={styles.spin}/>:<Search/>}Pesquisar</button></form>{loading?<div className={styles.state}><LoaderCircle className={styles.spin}/><strong>A pesquisar em todos os módulos…</strong></div>:!submitted?<div className={styles.state}><Search/><strong>Pesquisa toda a plataforma.</strong><p>Experimenta o nome de uma unidade curricular, um tema ou o título de um documento.</p></div>:results.length===0?<div className={styles.state}><Search/><strong>Sem resultados para “{submitted}”.</strong><p>Verifica a escrita ou utiliza termos mais abrangentes.</p></div>:<><div className={styles.panelHeader}><div><h2>Resultados para “{submitted}”</h2><p>Ordenados por relevância.</p></div><span className={styles.count}>{results.length} {results.length===1?"resultado":"resultados"}</span></div><div className={styles.results}>{results.map(item=><Link className={styles.result} href={item.href} key={`${item.type}-${item.id}`}><span className={styles.resultType}>{resultIcon(item.type)}</span><span><strong>{item.title}</strong><small>{item.description||item.meta||labels[item.type]||"Conteúdo"}</small></span><span className={styles.tags}><span className={styles.tag}>{labels[item.type]??item.type}</span><ArrowRight/></span></Link>)}</div></>}</section></div></AppShell></ModuleGuard></AuthGuard>}
