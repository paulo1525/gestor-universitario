"use client";
/* eslint-disable react-hooks/set-state-in-effect */

import Link from "next/link";
import { useCallback, useEffect, useState } from "react";
import { ArrowRight, BarChart3, BookOpen, ClipboardList, FileText, LoaderCircle, Megaphone, TrendingUp, Users, Vote } from "lucide-react";
import { AppShell } from "@/components/app-shell";
import { AppToast } from "@/components/app-toast";
import { AuthGuard } from "@/components/auth-guard";
import { ModuleGuard } from "@/components/module-guard";
import styles from "@/components/community-suite.module.css";

type Dashboard={metrics:{activeAnnouncements:number;openRequests:number;pendingMaterials:number;activePolls:number};engagement:Array<{label:string;value:number;total?:number}>;recent:Array<{id:string;title:string;description?:string;type:string;href?:string;createdAt?:string}>;units:Array<{id:string;name:string;code?:string;issues:number;events:number}>};
function normalize(raw:Record<string,unknown>):Dashboard{const metrics=(raw.metrics??{}) as Record<string,unknown>;const engagement=(raw.engagement??raw.moduleEngagement??[]) as Array<Record<string,unknown>>;const recent=(raw.recent??raw.recentActivity??[]) as Array<Record<string,unknown>>;const units=(raw.units??raw.unitOverview??[]) as Array<Record<string,unknown>>;return{metrics:{activeAnnouncements:Number(metrics.activeAnnouncements??metrics.active_announcements??0),openRequests:Number(metrics.openRequests??metrics.open_requests??0),pendingMaterials:Number(metrics.pendingMaterials??metrics.pending_materials??0),activePolls:Number(metrics.activePolls??metrics.active_polls??0)},engagement:engagement.map(item=>({label:String(item.label??item.name??"Módulo"),value:Number(item.value??item.count??0),total:item.total==null?undefined:Number(item.total)})),recent:recent.map(item=>({id:String(item.id),title:String(item.title??"Atividade"),description:item.description?String(item.description):undefined,type:String(item.type??"activity"),href:item.href?String(item.href):undefined,createdAt:item.createdAt?String(item.createdAt):undefined})),units:units.map(item=>({id:String(item.id),name:String(item.name??"Unidade curricular"),code:item.code?String(item.code):undefined,issues:Number(item.issues??item.openRequests??0),events:Number(item.events??item.upcomingEvents??0)}))}}
function icon(type:string){if(type.includes("poll"))return <Vote/>;if(type.includes("material")||type.includes("document"))return <FileText/>;if(type.includes("announcement"))return <Megaphone/>;return <ClipboardList/>}
export function CommunityAdminDashboard(){const[data,setData]=useState<Dashboard|null>(null),[loading,setLoading]=useState(true),[error,setError]=useState("");const load=useCallback(async()=>{setLoading(true);setError("");try{const response=await fetch("/api/dashboard",{cache:"no-store"});const raw=await response.json() as Record<string,unknown>&{error?:string};if(!response.ok)throw new Error(raw.error||"Não foi possível carregar o dashboard.");setData(normalize(raw))}catch(reason){setError(reason instanceof Error?reason.message:"Não foi possível carregar o dashboard.")}finally{setLoading(false)}},[]);useEffect(()=>{void load()},[load]);return <AuthGuard requireAdmin><ModuleGuard moduleKey="dashboard.analytics"><AppShell active="dashboard" breadcrumb="Dashboard"><div className={styles.page}><header className={styles.hero}><div className={styles.heroCopy}><span className={styles.heroIcon}><BarChart3/></span><div><span className="eyebrow">Visão operacional</span><h1>Dashboard administrativo</h1><p>Acompanha atividade, participação e tarefas pendentes em todos os módulos da aplicação.</p></div></div><div className={styles.heroActions}><button className="button button--secondary" type="button" onClick={()=>void load()} disabled={loading}><TrendingUp/>Atualizar dados</button></div></header>{error&&<AppToast kind="error" message={error} duration={0} onDismiss={()=>setError("")}/>} {loading?<section className={styles.panel}><div className={styles.state}><LoaderCircle className={styles.spin}/><strong>A calcular indicadores…</strong></div></section>:data&&<><section className={styles.statsGrid} aria-label="Indicadores principais"><Stat icon={<Megaphone/>} value={data.metrics.activeAnnouncements} label="Comunicados ativos"/><Stat icon={<ClipboardList/>} value={data.metrics.openRequests} label="Pedidos por tratar"/><Stat icon={<FileText/>} value={data.metrics.pendingMaterials} label="Materiais por moderar"/><Stat icon={<Vote/>} value={data.metrics.activePolls} label="Inquéritos ativos"/></section><div className={styles.dashboardGrid}><section className={styles.panel}><div className={styles.panelHeader}><div><h2>Atividade por módulo</h2><p>Volume relativo da atividade registada.</p></div></div>{data.engagement.length?<div className={styles.progressList}>{data.engagement.map(item=>{const percent=item.total?Math.min(100,Math.round(item.value/item.total*100)):Math.min(100,item.value);return <div className={styles.progressLine} key={item.label}><div><strong>{item.label}</strong><span>{item.value.toLocaleString("pt-PT")}</span></div><div className={styles.progressTrack}><span style={{width:`${percent}%`}}/></div></div>})}</div>:<div className={styles.state}><TrendingUp/><strong>Ainda não existem dados de atividade.</strong></div>}</section><section className={styles.panel}><div className={styles.panelHeader}><div><h2>Atividade recente</h2><p>Últimas ações relevantes na plataforma.</p></div></div>{data.recent.length?<div className={styles.sectionBody}>{data.recent.map(item=>{const content=<><span className={styles.listIcon}>{icon(item.type)}</span><span><strong>{item.title}</strong><small>{item.description??item.createdAt??""}</small></span>{item.href&&<ArrowRight/>}</>;return item.href?<Link className={styles.listItem} href={item.href} key={item.id}>{content}</Link>:<div className={styles.listItem} key={item.id}>{content}</div>})}</div>:<div className={styles.state}><ClipboardList/><strong>Sem atividade recente.</strong></div>}</section></div><section className={styles.panel}><div className={styles.panelHeader}><div><h2>Acompanhamento por unidade curricular</h2><p>Pedidos pendentes e próximos momentos académicos.</p></div><Link className="button button--secondary button--compact" href="/unidades-curriculares"><BookOpen/>Ver catálogo</Link></div>{data.units.length?<div className={styles.grid}>{data.units.map(item=><Link className={styles.card} href={`/unidades-curriculares/${encodeURIComponent(item.id)}`} key={item.id}><span className={styles.unitCode}>{item.code??"UC"}</span><h3>{item.name}</h3><div className={styles.metrics}><div className={styles.metric}><span>Pedidos</span><strong>{item.issues}</strong></div><div className={styles.metric}><span>Próximos eventos</span><strong>{item.events}</strong></div></div><span className={styles.linkHint}>Abrir unidade <ArrowRight/></span></Link>)}</div>:<div className={styles.state}><Users/><strong>Sem unidades para acompanhar.</strong></div>}</section></>}</div></AppShell></ModuleGuard></AuthGuard>}
function Stat({icon,value,label}:{icon:React.ReactNode;value:number;label:string}){return <article className={styles.statCard}><span className={styles.statIcon}>{icon}</span><span><strong>{value.toLocaleString("pt-PT")}</strong><small>{label}</small></span></article>}
